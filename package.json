{
  "name": "electroke",
  "version": "0.0.1",
  "description": "Electron charged Stroke - another mouse gesture program for Windows",
  "main": "index.js",
  "scripts": {
    "build": "node-gyp rebuild --target=1.3.5 --arch=ia32 --dist-url=https://atom.io/download/atom-shell && asar pack . build/asar/app.asar --unpack-dir build",
    "start": "electron ."
  },
  "author": "oxyflour <oxyflour@hotmail.com>",
  "license": "MIT",
  "devDependencies": {
    "nan": "^2.2.0"
  }
}
